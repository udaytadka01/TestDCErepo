<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PracticeGroupParentValidation</fullName>
    <active>false</active>
    <errorConditionFormula>AND(
  ISPICKVAL(Type, &apos;Practice Group&apos;),
  OR(
    ISBLANK(ParentId),
    AND(
      NOT(ISPICKVAL(Parent.Type, &apos;Organization&apos;)),
      NOT(ISPICKVAL(Parent.Type, &apos;Group&apos;))
    )
  )
)</errorConditionFormula>
    <errorMessage>Practice Groups must have a parent and can only have parents of the type &quot;Organization&quot; or &quot;Group&quot;</errorMessage>
</ValidationRule>
